version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Build started...
      - echo Nothing to build for Node.js

  post_build:
    commands:
      - echo Build completed.
